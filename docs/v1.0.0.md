
# 本地mysql数据库连接 [DONE]
- 已安装 `mysql2` 和 `dotenv`
- 已建立 `src/config/db.ts` 连接池
- 已添加 `/db-check` 路由用于验证连接
- 请在 `.env` 中配置您的本地数据库信息


# 创建一个用户表 用户登录使用jwt验证 用户uid采用纯数字的不要1，2，3，4这样自增 [DONE]

```sql
CREATE TABLE `user` (
  `uid` int(11) NOT NULL AUTO_INCREMENT,
  `username` varchar(255) NOT NULL,
  `password` varchar(255) NOT NULL,
  `email` varchar(255) DEFAULT NULL,
  `phone` varchar(255) DEFAULT NULL,
  `avatar` varchar(255) DEFAULT NULL,
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`uid`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```



# 统一接口请求的状态码 并备注不同状态码的含义 [DONE]

接口返回统一采用以下格式：

```json
{
  "code": 200,
  "message": "请求成功",
  "data": { ... }
}
```

### 状态码说明

| 状态码 | 含义 | 说明 |
| :--- | :--- | :--- |
| 200 | SUCCESS | 请求成功 |
| 400 | BAD_REQUEST | 请求参数有误，请检查输入 |
| 401 | UNAUTHORIZED | 身份验证失败，请重新登录 |
| 403 | FORBIDDEN | 权限不足，禁止访问 |
| 404 | NOT_FOUND | 请求的资源不存在 |
| 409 | CONFLICT | 资源冲突（如用户名已重复注册） |
| 500 | INTERNAL_ERROR | 服务器内部错误 |
